<h1><%= @task.name %></h1>
<%= link_to 'Edit task', edit_task_path(@task) %>
<%= link_to 'Delete', task_path(@task), method: :delete, data: { confirm: 'Are you shure you want to delete this task?' } %>
<p><%= @task.description  %></p>
<p><%= @task.created_at %></p>
<p><%= @task.updated_at %></p>